import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rekluzlabs.makotravelassist',
  appName: 'MakoRevive',
  webDir: 'dist'
};

export default config;
